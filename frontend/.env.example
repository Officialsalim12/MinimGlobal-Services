NEXT_PUBLIC_ROOT_DOMAIN=localhost
# Production with real DNS + Vercel domains (apex + studios/food/architect/clothing, or *):
# NEXT_PUBLIC_ROOT_DOMAIN=minimglobal.com
# Until that is set, production uses /businesses/{slug} paths on the apex host.
